# Browser / telefon kamerasi — WebRTC + WHIP ingest
logLevel: info

api: yes
apiAddress: :9997

rtsp: yes
rtspAddress: :8554

webrtc: yes
webrtcAddress: :8889
webrtcAllowOrigins: ["*"]
webrtcAdditionalHosts:
  - api.stream.vibrant.uz
  - stream.vibrant.uz
webrtcICEHostNAT1To1IPs:
  - __SERVER_IP__
webrtcLocalTCPAddress: :8189
webrtcICEServers2:
  - url: stun:stun.l.google.com:19302
  - url: stun:stun1.l.google.com:19302

pathDefaults:
  source: publisher
  runOnReady: curl -s -X POST "http://host.docker.internal:9084/hooks/publish?internal_secret=__MEDIA_HOOK_SECRET__" -d "name=$MTX_PATH" -d "source=whip"
  runOnReadyRestart: false
  runOnNotReady: curl -s -X POST "http://host.docker.internal:9084/hooks/publish_done?internal_secret=__MEDIA_HOOK_SECRET__" -d "name=$MTX_PATH"

paths:
  all_others:
