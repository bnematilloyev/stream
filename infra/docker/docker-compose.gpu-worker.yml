# GPU worker overlay — MinIO ni RunPod dan ko'rinishi uchun ochadi.
# NATS porti setup skripti docker-compose.prod.yml da 14222:4222 ga o'zgartiriladi.
services:
  minio:
    ports:
      - "19000:9000"
